<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.streammanager.client">

    <!-- Permissões necessárias para streaming e comunicação -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <!-- Permissões para captura de mídia -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Permissões para armazenamento (screenshots, cache) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" android:maxSdkVersion="32" />

    <!-- Recursos necessários -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:name=".StreamingApplication"
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_camera"
        android:label="Stream Manager"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Atividade principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Atividade de bloqueio de tela -->
        <activity
            android:name=".ScreenLockActivity"
            android:exported="false"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:screenOrientation="portrait"
            android:launchMode="singleTop">
        </activity>

        <!-- Serviço de streaming -->
        <service
            android:name=".StreamingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|camera|microphone" />

        <!-- Serviço de bloqueio de tela -->
        <service
            android:name=".ScreenLockService"
            android:enabled="true"
            android:exported="false" />

        <!-- Serviço de monitoramento de apps -->
        <service
            android:name=".AppMonitorService"
            android:enabled="true"
            android:exported="false" />

        <!-- Metadados de configuração -->
        <meta-data
            android:name="server_url"
            android:value="wss://stream-server.local:8443/ws" />

        <meta-data
            android:name="connection_timeout"
            android:value="30000" />

        <meta-data
            android:name="reconnect_delay"
            android:value="5000" />

        <meta-data
            android:name="heartbeat_interval"
            android:value="30000" />

        <!-- Configuração de segurança de rede -->
        <meta-data
            android:name="android.security.net.config"
            android:resource="@xml/network_security_config" />

    </application>

</manifest>